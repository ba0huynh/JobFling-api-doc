info:
  name: detail for worker
  type: http
  seq: 3

http:
  method: GET
  url: "{{routeDetail}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
