info:
  name: worker's detail
  type: http
  seq: 1

http:
  method: GET
  url: "{{routeDetail}}/workers{{workerId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
