info:
  name: employer job list
  type: http
  seq: 1

http:
  method: get
  url: "{{route}}/employer"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
