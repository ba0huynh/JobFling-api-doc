info:
  name: search
  type: http
  seq: 2

http:
  method: POST
  url: "{{route}}/search"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
