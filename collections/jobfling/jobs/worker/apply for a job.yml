info:
  name: apply for a job
  type: http
  seq: 8

http:
  method: POST
  url: "{{route}}/{{jobId}}/apply"
  headers:
    - name: x-locale
      value: ""
  body:
    type: json
    data: '{"idVisibilityConsent":false}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
