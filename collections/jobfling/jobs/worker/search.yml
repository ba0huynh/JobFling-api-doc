info:
  name: search
  type: http
  seq: 1

http:
  method: POST
  url: "{{route}}/search"
  body:
    type: json
    data: |-
      {
        "provinceCode":"91"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
