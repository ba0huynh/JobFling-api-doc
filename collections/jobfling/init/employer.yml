info:
  name: employer
  type: http
  seq: 2

http:
  method: POST
  url: "{{route}}/employer"
  body:
    type: json
    data: |-
      {
      //   "search" :{}
      }
  auth:
    type: basic

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
