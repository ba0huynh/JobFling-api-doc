info:
  name: worker leave shift
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{route}}/{{shiftId}}/apply"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
