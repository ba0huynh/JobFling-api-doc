info:
  name: worker leave shift
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/shifts/:id/apply"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
