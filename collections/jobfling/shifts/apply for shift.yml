info:
  name: apply for shift
  type: http
  seq: 3

http:
  method: POST
  url: "{{route}}/{{shiftId}}/apply"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
