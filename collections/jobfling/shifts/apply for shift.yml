info:
  name: apply for shift
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/shifts/:id/apply"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
