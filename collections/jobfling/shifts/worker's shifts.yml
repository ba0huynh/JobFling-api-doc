info:
  name: worker's shifts
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/shifts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
