info:
  name: shift detail employer
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/shifts/:id"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
