info:
  name: confirm
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{route}}/{{shiftId}}/confirm"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
