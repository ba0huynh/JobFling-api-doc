info:
  name: confirm
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/shifts/:id/confirm"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
