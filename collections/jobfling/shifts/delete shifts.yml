info:
  name: delete shifts
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{route}}"
  body:
    type: json
    data: |-
      {
      // array of strings
        "shiftIds":[], 
        "type" : "{{recurrenceActions}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
