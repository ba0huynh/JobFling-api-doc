info:
  name: get bank list
  type: http
  seq: 1

http:
  method: GET
  url: "{{route}}?query=mb"
  params:
    - name: query
      value: mb
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
