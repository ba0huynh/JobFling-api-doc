info:
  name: need-configure
  type: http
  seq: 3

http:
  method: GET
  url: "{{route}}/need-configure"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
