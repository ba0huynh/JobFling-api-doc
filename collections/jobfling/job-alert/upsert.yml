info:
  name: upsert
  type: http
  seq: 1

http:
  method: PUT
  url: "{{route}}"
  body:
    type: json
    data: ' {"radius":14,"categories":[],"alwaysAvailable":true}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
