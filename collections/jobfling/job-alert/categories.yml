info:
  name: categories
  type: http
  seq: 2

http:
  method: PUT
  url: "{{route}}/categories"
  body:
    type: json
    data: '{"ids":[]}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
