info:
  name: end call
  type: http
  seq: 4

http:
  method: POST
  url: "{{route}}/:id/call/end"
  params:
    - name: id
      value: ""
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
