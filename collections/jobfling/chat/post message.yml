info:
  name: post message
  type: http
  seq: 2

http:
  method: POST
  url: "{{route}}/:id/messages"
  params:
    - name: id
      value: ""
      type: path
  body:
    type: json
    data: |-
      {

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
