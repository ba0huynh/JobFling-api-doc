info:
  name: submitted
  type: http
  seq: 1

http:
  method: POST
  url: "{{route}}/submitted"
  body:
    type: json
    data: '{"challenges":["down","left","blink"]}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
