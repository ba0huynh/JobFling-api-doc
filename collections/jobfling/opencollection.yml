opencollection: 1.0.0

info:
  name: jobfling
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{token}}"
  variables:
    - name: recurrenceActions
      value: single | future | all
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
