info:
  name: get province from coordinate
  type: http
  seq: 1

http:
  method: GET
  url: "{{route}}/province"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
