info:
  name: google
  type: http
  seq: 3

http:
  method: GET
  url: "{{route}}/social/google?idToken={{idToken}}"
  params:
    - name: idToken
      value: "{{idToken}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
