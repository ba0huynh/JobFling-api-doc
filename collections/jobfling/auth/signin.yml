info:
  name: signin
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/signin"
  body:
    type: json
    data: |-
      {
        "email":{{email}},
        "password": {{password}} 
      }
  auth: inherit

runtime:
  variables:
    - name: email
      value: huynh8a0k5@gmail.com
    - name: password
      value: Huynhtanbao123@

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
