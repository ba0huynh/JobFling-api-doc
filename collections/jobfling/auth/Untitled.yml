info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/auth/signin"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
